# Namespace — one app-team's isolated "apartment" on the shared platform.
# TEMPLATE: audience-backend and audience-team are filled in by new-app.sh. Never edit the generated copy;
# edit THIS template so every app stays consistent.
apiVersion: v1
kind: Namespace
metadata:
  name: audience-backend
  labels:
    team: audience-team
    tier: backend
    managed-by: platform        # marks this as platform-generated, not hand-rolled
